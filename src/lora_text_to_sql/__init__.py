"""QLoRA text-to-SQL: shared library code.

Populated from Phase 1 onward (table materializer, prompt builder, and the
execution-match evaluation harness). Kept deliberately empty at Phase 0 --
nothing is built until the environment gate passes.
"""

__version__ = "0.1.0"
